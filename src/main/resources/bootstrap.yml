server:
  port: 8099

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/JHCTDEV/config_server.git
          search-paths: config-data
          username: ${USER_GIT}
          password: ${PASSWORD_GIT}

  security:
    user:
      name: root
      password: 123a


